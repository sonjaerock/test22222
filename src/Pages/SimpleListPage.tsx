import {ReactElement} from 'react';
import SimpleListContainer from "../Containers/SimpleListContainer";

const SimpleListPage = (): ReactElement | null => {
    return (
        <div>
            <SimpleListContainer></SimpleListContainer>
        </div>
    );
};

export default SimpleListPage;